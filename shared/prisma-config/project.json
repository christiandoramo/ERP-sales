{
  "name": "prisma",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "shared/prisma/src",
  "projectType": "library",
  "tags": [],
  "// targets": "to see all targets run: nx show project prisma --web",
  "targets": {
    "seed": {
      "executor": "nx:run-commands",
      "options": {
        "command": "ts-node -r ../../tsconfig-paths/register shared/prisma-config/src/lib/seed.ts"
      }
    }

  }
}
